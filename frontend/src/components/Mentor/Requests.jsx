import React from 'react'

const Requests = ({requests}) => {
  return (
    <div>
      <h1>Requests</h1>
      <div>
        {requests.map((request,img)=>{
            
        })}
      </div>
    </div>
  )
}

export default Requests
